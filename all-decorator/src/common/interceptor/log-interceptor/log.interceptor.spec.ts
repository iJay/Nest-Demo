import { LogInterceptorInterceptor } from './log.interceptor';

describe('LogInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new LogInterceptorInterceptor()).toBeDefined();
  });
});
